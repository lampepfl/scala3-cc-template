## Workflow scripts

- `latest-nightly.sc` and `scalaVersions.scala`: grabs the latest nightly version of the Scala compiler. Requires `scala-cli`.
  Comes from https://github.com/VirtusLab/community-build3, licenses follow accordingly (Apache 2.0).

